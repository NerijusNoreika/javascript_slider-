{
  "name": "javascript-slider",
  "version": "1.0.0",
  "scripts": {
    "build": "babel js -d lib",
    "gulp": "gulp"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "gulp": "^4.0.2",
    "gulp-cli": "^2.0.1",
    "gulp-sass": "^4.0.1"
  }
}
